<swiper autoplay={{autoplay}} interval={{interval}} duration={{duration}} circular={{circular}}>
  <block wx:for={{imgUrl}}>
    <swiper-item>
      <image src={{item}} height="200rpx"/>
    </swiper-item>
  </block>
</swiper>